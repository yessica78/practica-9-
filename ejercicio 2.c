#include<stdio.h>
int main(){
	int contador, suma=0;
	for(contador=0; contador=100; contador ++)
{	
	printf("El numero es:%d\n",contador);
	suma=suma+contador;
}
printf("El resultado total es:%d",suma);
return 0;
}

